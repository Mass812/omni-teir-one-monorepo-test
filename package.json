{
  "name": "ts-omni",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:all": "npx lerna exec npm i",
    "build:all": "npx lerna exec npm run tsc",
    "file": "npx lerna exec npm run file",
    "clean": "npx lerna run clean",
    "test": "npx lerna run test",
    "graph": "npx nx graph"
  },
  "devDependencies": {
    "@types/node": "18.11.15",
    "lerna": "6.1.0",
    "typescript": "4.9.4"
  },
  "dependencies": {
    "ts-node": "10.9.1"
  }
}
